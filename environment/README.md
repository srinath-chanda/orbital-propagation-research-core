# Recorded environment

`python-environment-paper1.json` records the operating system and interpreter
used for the production run. `requirements-paper1-windows.txt` is the complete
package snapshot captured from that environment.

The top-level `requirements.txt` contains supported dependency ranges for new
installations. The frozen environment file should be used when exact package
version matching is required.
